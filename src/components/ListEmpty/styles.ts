import { SxProps } from "@mui/material";

export const listEmptyStyles = (): SxProps => {
  return {
    padding: "2rem",
    background: "#e0f2f1",
    minHeight: "100vh",
    fontWeight: 900,
    fontSize: "16px",
    color: "#4db6ac",
    margin: "0 auto",
  };
};
